import { TileContainerDirective } from './tile-container.directive';

describe('TileContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new TileContainerDirective();
    expect(directive).toBeTruthy();
  });
});
